# FastAPI Lab

- Basic repo setup
    - .gitignore
- Create the repo structure

  ```
  app/
  ├── api.py          - FastAPI app
  ├── gunicorn.py     - WSGI script
  └── schemas.py      - API model schemas
  ```

